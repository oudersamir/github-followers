import { ResumePipePipe } from './resume-pipe.pipe';

describe('ResumePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ResumePipePipe();
    expect(pipe).toBeTruthy();
  });
});
